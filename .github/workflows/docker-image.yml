name: Build and Push Docker Image
on:
  push:
    branches:
      - main
  workflow_call:  # Allows this workflow to be reused by other workflows
    inputs:
      dockerfile:
        description: 'Path to the Dockerfile'
        required: true
        type: string
        default: './Dockerfile'
      dockerhub_repo:
        description: 'Docker repo name'
        required: true
        type: string
        default: 'venkatalakshmi0448/my-image'
      tags:
        description: 'Tag for the Docker image'
        required: true
        type: string
        default: 'latest'
         
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}

    - name: Debug - Print repository and tag
      run: |
        echo "Docker Hub Repository: ${{ inputs.dockerhub_repo }}"
        echo "Tag: ${{ inputs.tags }}"

    - name: Build and push Docker image
      uses: docker/build-push-action@v3
      with:
        context: .
        file: ${{ inputs.dockerfile }} 
        push: true
        tags: ${{ inputs.dockerhub_repo }}:${{ inputs.tags }}

    - name: Logout from DockerHub
      run: docker logout
